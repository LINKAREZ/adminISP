# Admin ISP - Docker Compose para VPS
# Uso: docker compose up -d

services:
  app:
    build: .
    container_name: adminisp-app
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - .:/var/www/html
    environment:
      - APP_NAME=${APP_NAME:-AdminISP}
      - APP_ENV=production
      - DB_HOST=db
      - DB_DATABASE=${DB_DATABASE:-adminisp}
      - DB_USERNAME=${DB_USERNAME:-adminisp}
      - DB_PASSWORD=${DB_PASSWORD:-adminisp%}
    networks:
      - app
    depends_on:
      - db

  nginx:
    image: nginx:alpine
    container_name: adminisp-nginx
    restart: unless-stopped
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - .:/var/www/html:ro
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./docker/nginx/ssl:/etc/nginx/ssl:ro
    networks:
      - app
    depends_on:
      - app

  db:
    image: mysql:8.0.32
    container_name: adminisp-db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${DB_DATABASE:-adminisp}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-adminisp%}
      MYSQL_USER: ${DB_USERNAME:-adminisp}
      MYSQL_PASSWORD: ${DB_PASSWORD:-adminisp%}
    volumes:
      - dbdata:/var/lib/mysql
    networks:
      - app

  # Adminer: interfaz web para ver/administrar la base de datos
  adminer:
    image: adminer:latest
    container_name: adminisp-adminer
    restart: unless-stopped
    ports:
      - '8080:8080'
    environment:
      ADMINER_DEFAULT_SERVER: db
    networks:
      - app
    depends_on:
      - db

networks:
  app:
    driver: bridge

volumes:
  dbdata:
